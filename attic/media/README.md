# Unreferenced media, parked for review

Moved out of `media/` on 2026-07-30: nothing in the repo (lectures, docs
pages, examples, tikz sources, notebooks, generated output) references
these files. Kept here rather than deleted so they can be reviewed;
delete this directory when done. `git log --follow attic/media/<file>`
shows each file's history.
