\input{tikz/fig_header.tex}

% Close-up of the drain end in saturation: the field from the depleted
% gap between the pinch-off point and the drain sweeps electrons across.

\begin{tikzpicture}[scale=0.85]
  \input{tikz/mos_lib.tex}
  \drainzoomframe

  % V_DS-sat and ~V_DS marks with the x span (only this figure reads x)
  \draw[thick] (4.2,4.68) -- (4.2,4.92);
  \node[anchor=east, scale=0.8, fill=white, inner sep=1pt] at (4.1,4.82) {$V_{DS\text{-}sat}$};
  \draw[thick] (6.6,4.68) -- (6.6,4.92);
  \node[anchor=west, scale=0.8, fill=white, inner sep=1pt] at (6.7,4.82) {$\sim V_{DS}$};
  \draw[<->, thin] (4.3,4.8) -- (6.5,4.8);
  \node[anchor=south, scale=0.8] at (5.4,4.88) {$x$};

  % field from the gate down through the oxide
  \foreach \x in {1.3,2.0,2.7}{
    \draw[fieldy, thick, ->] (\x,5.1) -- (\x,4.7);
  }

  % strong lateral field across the depleted gap: parallel field lines
  % from the drain towards the pinch-off point
  \foreach \y/\xe in {4.25/4.7, 3.85/4.95, 3.45/5.2}{
    \draw[fieldy, thick, ->] (6.55,\y) -- (\xe,{\y+0.12});
  }
  \node[anchor=west, fill=white, inner sep=1.5pt] at (4.4,2.75) {$\vec{E}_{DEPLETION} = $ High!};
  \draw[thin] (5.3,2.95) -- (5.6,3.4);

  % weak field inside the drain
  \foreach \x in {7.4,8.2,9.0}{
    \draw[fieldy, thick, ->] (\x,2.05) -- (\x,1.65);
  }
  \node[anchor=west, fill=white, inner sep=1.5pt] at (7.9,3.95) {$\vec{E}_{DRAIN} = $ Low};
  \draw[fieldy, thick, ->] (8.2,3.7) -- (8.2,3.35);
  \draw[fieldy, thick, ->] (9.0,3.7) -- (9.0,3.35);

  % an electron swept across the gap, above the field lines
  \node[echarge, anchor=center, scale=1.15] at (3.55,4.42) {$\ominus$};
  \draw[echarge, line width=1.3pt, ->] (3.85,4.44) -- (5.3,4.5);

\end{tikzpicture}
\end{document}
