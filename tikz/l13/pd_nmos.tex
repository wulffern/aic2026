\input{tikz/fig_header.tex}

% Pull-down networks: NMOS in series and NMOS in parallel, from the
% output down to ground. The truth tables live in the slide beside this.

\begin{circuitikz}[american, thick, transform shape, circuit ee IEC]

  \input{tikz/ckt_lib.tex}

  % ---- series ----
  \draw (0,0) \vground;
  \draw (0,0) \lvnmos{MSB}{B};
  \draw (0,\grid) \lvnmos{MSA}{A};
  \draw (0,{2*\grid}) \portOut{Y};
  \node[anchor=north, color=black!60] at (0.5,-0.7) {series};

  % ---- parallel ----
  \def\xa{5.2}
  \def\xb{7.4}
  \draw (\xa,0) \vground;
  \draw (\xb,0) \vground;
  \draw (\xa,0) \lvnmos{MPA}{A};
  \draw (\xb,0) \lvnmos{MPB}{B};
  \draw (\xa,\grid) -- (\xb,\grid);
  \fill (\xa,\grid) circle (0.075);
  \draw (\xb,\grid) \portOut{Y};
  \node[anchor=north, color=black!60] at ({(\xa+\xb)/2},-0.7) {parallel};

\end{circuitikz}
\end{document}
