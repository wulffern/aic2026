\input{tikz/fig_header.tex}

% Clock gating: a latch based clock gate cell (top), and stopping the
% clock to a registered logic cloud when nothing changes (bottom).

\newcommand{\cgdff}{%
  \draw[blue] (0,0) rectangle (1.5,2.0);
  \node[blue, anchor=west] at (0.08,1.55) {D};
  \node[blue, anchor=east] at (1.42,1.55) {Q};
  \node[blue, anchor=east] at (1.42,0.45) {$\overline{\mbox{Q}}$};
  \draw[blue] (0.55,0) -- (0.75,0.25) -- (0.95,0);
  \draw[blue] (1.5,0.45) -- (1.9,0.45);
}
%
% The source PDF is a crop: only the clock gate is inside
% media/l16/stop_clock.pdf's CropBox. The registers and cloud below it
% on that page are a different figure (media/logic.pdf).

\begin{circuitikz}[thick]

  % ----------------------------------------------------------------
  % clock gate cell
  % ----------------------------------------------------------------
  \begin{scope}[shift={(1.2,10.5)}]
    \node[draw=blue, rectangle, align=center, font=\bfseries,
          text=blue, minimum height=1.1cm, inner xsep=6pt]
      (en) at (1.1,2.55) {Enable\\Logic};
    \draw[->] (-0.6,2.55) -- (en.west);

    % latch
    \draw[blue] (3.0,0.6) rectangle (4.6,3.0);
    \node[blue, anchor=west] at (3.08,2.55) {D};
    \node[blue, anchor=east] at (4.52,2.55) {Q};
    \node[blue, anchor=east] at (4.52,1.05) {$\overline{\mbox{Q}}$};
    \node[blue, scale=1.6] at (3.75,1.9) {L};
    \draw[blue] (3.55,0.6) -- (3.75,0.85) -- (3.95,0.6);
    \draw[blue] (4.6,1.05) -- (5.0,1.05);
    \draw (en.east) -- (3.0,2.55);

    % AND gate
    \node[and port] (cga) at (7.6,2.3) {};
    \draw (4.6,2.55) -- (6.3,2.55) -- (cga.in 1);
    \draw (cga.out) to[short,-o] ++(0.8,0);
    \node[font=\bfseries, anchor=south] at (8.7,2.6) {Clk out};

    % clock in: to the latch gate and the AND gate
    \draw (2.5,-0.55) to[short,o-] (3.75,-0.55) -- (3.75,0.6);
    \fill (3.75,-0.55) circle (0.06);
    \draw (3.75,-0.55) -- (6.7,-0.55) -- (6.7,2.0) -- (cga.in 2);
    \node[font=\bfseries, anchor=east] at (2.35,-0.25) {Clk in};
  \end{scope}

\end{circuitikz}
\end{document}
