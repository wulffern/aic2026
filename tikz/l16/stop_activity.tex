\input{tikz/fig_header.tex}

% Stopping the activity, drawn as the same registers-cloud-registers as
% digital_ff_comb so the two slides read as one: the data path out of
% the input registers is cut (the red break), the cloud's inputs are
% tied to a constant, and the alpha in the dynamic power equation goes
% to zero while the flip-flops still see their clock. Cutting data, not
% clock, is the point: nothing has to be re-synchronized afterwards.

\begin{circuitikz}[thick]
  \input{tikz/ckt_lib.tex}

  \newcommand{\dffbox}{
    \draw (0,0) rectangle (1.8,2.4);
    \node[anchor=west] at (0.05,1.95) {$D$};
    \node[anchor=east] at (1.75,1.95) {$Q$};
    \node[anchor=east] at (1.75,0.55) {$\overline{Q}$};
    \draw (1.8,0.55) -- (2.3,0.55);
    \draw (0.5,0) -- (0.8,0.25) -- (1.1,0);
  }

  \begin{scope}[shift={(0,4.5)}] \dffbox \end{scope}
  \begin{scope}[shift={(0,-1.5)}] \dffbox \end{scope}
  \node at (0.9,2.1) {$\vdots$};

  \draw (-1.2,6.45) node[anchor=east] {$A[0]$} -- (0,6.45);
  \draw (-1.2,0.45) node[anchor=east] {$A[N]$} -- (0,0.45);

  \begin{scope}[shift={(10.4,4.5)}] \dffbox \end{scope}
  \begin{scope}[shift={(10.4,-1.5)}] \dffbox \end{scope}
  \node at (11.3,2.1) {$\vdots$};

  \draw (12.2,6.45) -- (13.4,6.45) node[anchor=west] {$X[0]$};
  \draw (12.2,0.45) -- (13.4,0.45) node[anchor=west] {$X[N]$};

  \draw[blue] plot [smooth cycle, tension=1]
    coordinates {(3.6,3.0) (3.9,5.0) (4.6,6.2) (5.6,6.6) (6.6,6.9)
                 (7.8,6.4) (8.6,5.4) (8.9,3.4) (8.7,1.4) (8.0,-0.2)
                 (6.6,-0.9) (5.2,-0.6) (4.2,0.4) (3.7,1.6)};

  \begin{scope}[scale=0.8]
    \draw (6.9,6.5) \cicNor{cga};
    \draw (7.08,3.88) \cicInv{cgb};
    \draw (7.01,1.25) \cicNand{cgc};
  \end{scope}

  % The cut: the register outputs stop short of the cloud, and the
  % cloud inputs are tied off to a constant.
  \draw (1.8,6.45) -- (2.6,6.45);
  \draw (3.46,6.45) -- (4.4,6.45);
  \draw[red] (2.85,6.1) -- (3.15,6.8);
  \draw (1.8,0.45) -- (2.6,0.45);
  \draw (3.46,0.45) -- (4.4,0.45);
  \draw[red] (2.85,0.1) -- (3.15,0.8);
  \node[red, anchor=south] at (3.0,7.0) {cut};
  % the cloud side of each cut wire is tied to a constant
  \draw (3.4,6.45) circle (0.06);
  \node[anchor=north] at (3.4,6.2) {\small $0$};
  \draw (3.4,0.45) circle (0.06);
  \node[anchor=north] at (3.4,0.2) {\small $0$};

  % alpha = 0 inside the now quiet cloud
  \node[blue, anchor=north] at (6.2,0.2) {$\alpha = 0$};

  \draw (-3.0,-2.8) -- (14.8,-2.8);
  \draw (11.2,-1.5) -- (11.2,-2.8);
  \draw (0.8,-1.5) -- (0.8,-2.8);
  \draw (0.8,4.5) -- (0.8,3.5) -- (-3.0,3.5) -- (-3.0,-2.8);
  \draw (11.2,4.5) -- (11.2,3.5) -- (14.8,3.5) -- (14.8,-2.8);
  \fill (0.8,-2.8) circle (0.075);
  \fill (11.2,-2.8) circle (0.075);
  \node[anchor=east] at (-3.2,-2.8) {Clk};

\end{circuitikz}
\end{document}
