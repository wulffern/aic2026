\input{tikz/fig_header.tex}

% My life, reconstructed in TikZ from the drawio original (media/timeline.svg).
% Geometry (label and leader positions) is a faithful transcription of that
% file; the events and their placement are unchanged.

% The picture is thirty units wide, so at text width the labels are
% scaled far down: they are set large here to survive that.
% The picture is thirty units wide - as one row in a book that makes
% every label unreadable. The body is defined once (a faithful
% transcription of the drawio original) and drawn twice through
% clipping windows: 1976-2006 on the top row, 2006-2026 below it.
% Nothing in the transcription moves; only what is visible changes.

\input{tikz/timeline_body.tex}

\begin{tikzpicture}[line width=0.9pt]
  \tikzset{ev/.style={font=\normalsize, align=center, inner sep=1.5pt},
           lead/.style={->, line width=0.9pt},
           yr/.style={font=\normalsize},
           nordic/.style={ev, blue},
           nordiclead/.style={lead, blue}}

  %- top row: the years up to 2006
  \begin{scope}
    \clip (-0.4,-3.1) rectangle (17.8,4.6);
    \timelinebody
  \end{scope}

  %- bottom row: 2006 onwards, brought back to the left and down
  \begin{scope}[shift={(-17.35,-9.0)}]
    \begin{scope}
      %- taller than the top row: the 2026 label sits on its own line
    \clip (17.8,-3.95) rectangle (30.8,4.6);
      %- the Nordic band and its label live in the body's coordinates
      \timelinebody
    \end{scope}
  \end{scope}

\end{tikzpicture}
\end{document}
